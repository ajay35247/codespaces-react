export function predictLoadMatch({ origin, destination, truckType, weight }) {
  return {
    suggestedTruckId: 'TRK-101',
    matchScore: 92,
    estimatedDistanceKm: 820,
    estimatedFreight: 54000,
  };
}
