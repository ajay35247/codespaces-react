<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose handles all UI theming; this XML theme is only used by the
         system window before the Compose tree is drawn. -->
    <style name="Theme.SpeedyTrucks" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
